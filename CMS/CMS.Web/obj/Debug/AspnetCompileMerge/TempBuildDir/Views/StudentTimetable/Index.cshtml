@model IEnumerable<CMS.Domain.Storage.Projections.StudentTimetableViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<h2>Student Exam Timetable List</h2>

<p>
    @Html.ActionLink("Create Exam Timetable", "Create")
</p>

<div class="panel panel-default">
    <div class="panel-body">
        @Html.Partial("_MVCGridToolbar", new CMS.Web.Models.MVCGridToolbarModel()
   {
       MVCGridName = "StudentTimetableGrid",
       PageSize = true,
       ColumnVisibility = true,
       Export = true,
       GlobalSearch = false
   })
        @Html.MVCGrid("StudentTimetableGrid", new { userRole = ViewBag.userId })
    </div>
</div>